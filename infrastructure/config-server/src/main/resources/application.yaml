spring:
  application:
    name: config-server

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///E:/CargoFlow/config-repo

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info