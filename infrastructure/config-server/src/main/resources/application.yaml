spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:///E:/CargoFlow/config-repo
          default-label: main

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info